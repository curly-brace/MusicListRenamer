<Window x:Class="MusicRenamer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicRenamer"
        mc:Ignorable="d"
        Title="Renamer" Height="150" Width="250" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Number of Digits:" />
        <TextBox x:Name="DigitsTxt" Grid.Column="1" Width="64" Text="3" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button x:Name="RemoveBtn" Content="Remove" Grid.Row="1" Margin="10" Click="RemoveBtn_Click"/>
        <Button x:Name="AddBtn" Content="Add" Grid.Row="1" Grid.Column="1" Margin="10" Click="AddBtn_Click"/>
    </Grid>
</Window>
